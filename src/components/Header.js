import React from "react";
import "./Header.css";

const Header = () => {
  return (
    <div
      style={{
        color: "white",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <h2>WEATHERAPP.</h2>
    </div>
  );
};

export default Header;
